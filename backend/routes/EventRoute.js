// const express = require("express");
// const router = express.Router();
// const { getAllEvents, getEventDetails, addEventReview, getEventReviews, bookEvent } = require("../controllers/EventController");

// router.get("/", getAllEvents); // Retrieve a list of events
// router.get("/:id", getEventDetails); // Retrieve event details
// router.post("/:id/reviews", addEventReview); // Add a review for an event
// router.get("/:id/reviews", getEventReviews); // Get reviews for a specific event
// router.post("/:id/bookings", bookEvent); // Book an event

// module.exports = router;
